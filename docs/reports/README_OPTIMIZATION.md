# PM Digital Employee - 优化版

## 项目概述

PM Digital Employee（项目经理数字员工）是为国有大行科技子公司项目管理部开发的飞书基项目管理智能助手。项目通过飞书作为唯一入口，实现项目管理智能化。

## 本次优化改进内容

### 1. 异常处理与错误码系统
- 重构了异常处理系统，实现了分级错误码机制
- 新增银行级安全的异常处理流程
- 统一了API响应格式，增强了trace_id追踪能力

### 2. 数据库连接池与事务管理
- 优化了数据库连接池配置，增加了连接预检测和回收机制
- 实现了只读事务和读写事务分离
- 增强了事务处理的稳定性和性能

### 3. API限流与并发控制
- 实现了基于用户的API限流中间件
- 添加了并发控制机制，防止系统过载
- 提供了健康检查端点用于监控限流状态

### 4. 安全与合规性增强
- 增加了输入参数校验和XSS防护
- 实现了敏感数据脱敏机制（手机号、邮箱、身份证、银行卡等）
- 完善了审计日志和内容合规检查

### 5. AI服务优化
- 添加了Token计数和成本计算功能
- 实现了结构化Prompt模板系统
- 增加了熔断和重试机制提升AI服务稳定性

### 6. 前端卡片安全
- 实现了飞书卡片内容的安全转义
- 添加了卡片构建工具，提升开发效率

### 7. 数据库查询优化
- 优化了项目服务中的N+1查询问题
- 使用JOIN和聚合函数提升查询性能
- 添加了复合索引提升查询速度

## 技术架构

### 9层架构设计
```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 接入层        │ 飞书 Bot Webhook、Callback、File Upload    │
├─────────────────────────────────────────────────────────────────┤
│ 2. API网关层     │ FastAPI、验签、幂等、trace_id、异常处理       │
├─────────────────────────────────────────────────────────────────┤
│ 3. 会话上下文层  │ 用户/群/项目上下文、对话状态机、多轮补参     │
├─────────────────────────────────────────────────────────────────┤
│ 4. 权限隔离层    │ 用户-项目权限、群-项目绑定、Skill权限校验    │
├─────────────────────────────────────────────────────────────────┤
│ 5. 编排层        │ Intent Router、Skill Registry、Orchestrator  │
├─────────────────────────────────────────────────────────────────┤
│ 6. AI能力层      │ LLM Gateway、Prompt Manager、RAG、安全防护   │
├─────────────────────────────────────────────────────────────────┤
│ 7. Skill插件层   │ 13个核心Skill，BaseSkill规范，manifest      │
├─────────────────────────────────────────────────────────────────┤
│ 8. 集成适配层    │ 项目管理、财务、DevOps、OA、飞书适配器      │
├─────────────────────────────────────────────────────────────────┤
│ 9. 数据层        │ PostgreSQL、Redis、pgvector、Celery         │
└─────────────────────────────────────────────────────────────────┘
```

## 安全特性

### 输入验证
- SQL注入防护
- XSS攻击防护
- 参数长度限制
- 敏感词汇过滤

### 数据保护
- 敏感数据自动脱敏
- 日志脱敏处理
- 传输加密
- 访问权限控制

### 服务保障
- API限流机制
- 并发控制
- 熔断降级
- 容错重试

## 部署说明

### 环境准备
```bash
# 安装依赖
pip install -r requirements.txt

# 设置环境变量
cp .env.example .env
# 编辑 .env 文件配置各项参数
```

### 本地开发
```bash
# 运行开发服务器
python -m app.main

# 或使用 uvicorn（热重载）
uvicorn app.main:app --reload --port 8000
```

### Docker部署
```bash
# 启动完整服务栈
docker-compose up -d

# 查看日志
docker-compose logs -f app

# 停止服务
docker-compose down
```

## API接口

### 主要端点
- `/` - 根路径
- `/docs` - API文档
- `/health` - 健康检查
- `/api/v1/projects` - 项目管理
- `/api/v1/skills` - 技能列表
- `/lark/webhook/event` - 飞书事件回调
- `/lark/callback` - 飞书回调验证

## 测试

```bash
# 运行所有测试
pytest

# 运行指定测试文件
pytest tests/test_main.py -v

# 生成覆盖率报告
pytest --cov=app tests/ --cov-report=html
```

## 监控与运维

### 性能监控
- 响应时间监控
- 并发数监控
- API限流状态
- 数据库连接池状态

### 日志系统
- 结构化日志输出
- Trace ID追踪
- 错误日志分级
- 审计日志记录

## 开发规范

### 代码规范
- 遵循 PEP 8 编码规范
- 使用 type hint 进行类型注解
- 统一的异常处理模式
- 安全的输入验证机制

### 安全开发
- 输入数据必须经过验证
- 输出数据需要进行转义
- 敏感信息禁止明文存储
- 权限控制严格执行

## 版本信息

**版本**: 1.0.0  
**发布时间**: 2026年4月  
**优化内容**: 安全性、稳定性、性能全面提升